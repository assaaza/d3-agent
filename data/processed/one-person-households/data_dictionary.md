# 데이터 사전 — 1인 가구 비율, 전처리 결과

**원본**: `data/raw/one-person-households/one_person_household_ratio_2000_2025.csv`
**처리 결과**: `data/processed/one-person-households/one_person_household_ratio_clean.csv`
**처리일**: 2026-09-02

## 전처리 체크리스트 (탐지 → 적용 결과)

| 항목 | 탐지 결과 | 적용 여부 |
|---|---|---|
| 컬럼명 정리 | 문제 없음 | 미적용 |
| 빈 행/열 제거 | 없음 | 미적용 |
| 중복 행 제거 | 0개 | 미적용 |
| 상수 컬럼 분리 | 4개(unit, source_org, source_table_id, source_table_name) | **적용 — 데이터 사전으로 이동** |
| 결측치 처리 | 없음 | 미적용 |
| 숫자형 타입 확인 | 4개 컬럼 모두 정상 | 확인 완료 |
| 이상치 탐지(IQR) | 2000년(15.5%)이 나머지 값들에 비해 낮음 | **표시만, 삭제하지 않음** — 실제 초기 조사값 |
| 재구성(long→wide) | 그룹 구분 컬럼이 없어 이미 tidy 상태 | 불필요 |

## 데이터셋 메타데이터 (상수 컬럼에서 분리됨)

- **단위(unit)**: %
- **출처 기관(source_org)**: 국가데이터처(통계청, ORG_ID=101)
- **원본 통계표 ID(source_table_id)**: DT_1YL21161
- **원본 통계표명(source_table_name)**: 1인가구비율(시도/시/군/구)

## 처리된 컬럼 설명

| 컬럼명 | 설명 | 타입 |
|---|---|---|
| year | 조사 연도 (2000, 2005, 2010, 2015, 2016~2025 매년) | 정수 |
| one_person_households | 1인 가구 수 | 정수(가구) |
| total_households | 일반가구 총수 | 정수(가구) |
| one_person_household_ratio_pct | 1인 가구 비율 (= 1인가구/일반가구×100) | 실수(%) |

## 이상치 참고 (삭제하지 않고 표시만 함)

- **2000년 15.5%** — 이후 연도들과 비교해 뚜렷하게 낮은 값. 오류가 아니라 조사 시작 시점의 실제 수치로, 25년간의 장기 증가 추세를 보여주는 값이므로 절대 삭제하면 안 됨.

## 건강(성인 비만율) 데이터와 비교 — 재현성 검증 메모

- 같은 체크리스트 로직(pandas 프로파일링)을 코드 수정 없이 그대로 적용함
- 이 데이터셋은 성별 같은 하위 그룹이 없어 재구성(long→wide) 단계가 자동으로 "불필요"로 판정됨 — 체크리스트가 데이터 구조에 따라 다르게 반응하는 것을 확인
- 상수 컬럼 개수만 다를 뿐(6개 vs 4개) 나머지 로직은 동일하게 작동함
